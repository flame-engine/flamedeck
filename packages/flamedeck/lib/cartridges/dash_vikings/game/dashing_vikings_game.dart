import 'package:flutter/material.dart';

class DashingVikings extends StatelessWidget {
  const DashingVikings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Placeholder')),
    );
  }
}
